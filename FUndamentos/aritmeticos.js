const [a,b,c,d] = [3,5,1,15]

const soma = a+ b+ c + d  //sintaxe 
const subtracao = c-d
const multiplicacao = a * b
const divisao = b / d
const resto = b%d

console.log(soma)
console.log(subtracao)
console.log(multiplicacao) //se colocar (-multiplicacao) ele deixa o resultado negativo. Tornando ele unario
/console.log(divisao)
console.log(resto)
//sintaxe infix , binario ope1 + ope2
//sintaxe postfix / prefix  = d++, ++d
// operador unario -a, +a
